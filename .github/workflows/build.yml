name: Build ZMK firmware
on:
  workflow_dispatch:
  push:
    paths-ignore:
      - "keymap-drawer/**"

jobs:
  build:
    # Pinned to stable version to match west.yml revision
    # Update this to match the revision in config/west.yml
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@3.2

